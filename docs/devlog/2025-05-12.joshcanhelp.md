I want to finish off v0.13.0 and maybe try out Claude code to see how that works. 

I see that I already have a 13 I guess?

- https://github.com/PersonalDataPipeline/pdpl-cli/releases/tag/v0.13.0
- https://github.com/PersonalDataPipeline/pdpl-cli/commit/c0359102b8651f31323484854616681788016bd9

I guess I just forgot to `npm publish`. OK. 

Going forward, I'd like to publish release information to GitHub and/or a changelog file. I don't want to spend a ton of time on this, either. I remember trying to get this right a long time ago and it was a pain. So I'm going to go with this script:

https://github.com/lob/generate-changelog

... and not worry about previous versions. I'll work through all that once 14 is ready and not worry about previous versions. 

Just saw that, once again, the script stopped running correctly. I moved the env and config files and was only testing with the `./bin` script, not the one in cron. This *definitely* needs some kind of alerting that lets users know when something goes wrong, otherwise data could go missing (like [GitHub events](https://docs.github.com/en/rest/activity/events?apiVersion=2022-11-28) that only provides data for 30 days with no way to get older data). 

---

Thinking about data transformation ... I feel like I've been trying to boil the ocean so let's look at this simply:

I need a way to execute custom TS/JS to transform data from one table to another. Secondarily, I want a local API to do that so folks don't have to write custom SQL. 

Let's just do the first first. 